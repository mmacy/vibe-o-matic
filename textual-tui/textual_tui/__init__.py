"""Textual TUI package for vibe-o-matic.

This package provides a terminal user interface (TUI) built with the Textual
framework for the vibe-o-matic suite of solo tabletop RPG tools. The TUI offers
an interactive command-line experience with rich formatting and keyboard navigation.

The main application can be launched via the `vibe-tui` command or by running
the app module directly.

Example:
    Launch the TUI from command line::

        $ vibe-tui

    Or run as a Python module::

        $ python -m textual_tui.app
"""

__version__ = "0.1.0"
